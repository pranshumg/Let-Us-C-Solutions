1. Use redirection at the command line:
```
./a.out < input.txt > output.txt
```

2. Use redirection and keyboard input:
```
./a.out > newfile.txt
```

3. Again use redirection:
```
./a.out < existingfile.txt
```