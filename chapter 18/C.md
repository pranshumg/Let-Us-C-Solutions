**(a)** 2. ```gets(str);```

**(b)** 1. ```scanf()```

**(c)** 1. Characters, format specifications, and escape sequences

**(d)** 4. Specify how many columns should be used to print the number

**(e)** ```printf("%-25s %-25s %8.2f\n", book, author, price);```
